# Codex 项目协作说明

## 回答语言

始终使用简体中文回复。

## 项目定位

这是陈萍的个人 UI/UX 设计师作品集网站，用于展示个人介绍、项目案例、能力标签、简历与作品集 PDF。后续修改应优先服务于作品展示、招聘沟通和设计合作转化。

## 协作原则

- 修改前先理解现有结构，不要随意重写整站。
- 优先保持当前视觉语言：清爽、专业、柔和、有设计感，但不要把页面做成过度装饰的模板站。
- 保持移动端和桌面端都可用，不能只看电脑宽屏效果。
- 所有资源路径要考虑 GitHub Pages 或静态部署环境，优先使用项目内相对路径。
- 不要删除用户已有素材、页面、记录或未提交改动，除非用户明确要求。
- 新增说明文档时要写得可执行，避免只写抽象原则。

## 技术约定

- 当前项目使用 Vite + React，同时保留了部分静态 HTML 页面。
- React 入口主要在 `src/App.jsx`、`src/main.jsx`、`src/styles.css`。
- 项目数据主要在 `src/data/projects.js`。
- 静态资源集中在 `assets/` 与 `public/assets/`。
- PDF 文件集中在 `files/` 与 `public/files/`。
- `vite.config.js` 中的 `base: './'` 是为了让构建后的资源使用相对路径，适配 GitHub Pages 等静态部署场景，非必要不要改成 `/`。

## 修改前检查

每次开始改网站前，先确认：

- 这次修改影响的是首页、详情页、静态 HTML，还是 React 版本。
- 是否会影响图片、PDF、样式文件等资源路径。
- 是否会影响手机端布局。
- 是否会改变现有内容表达、个人信息、项目案例顺序。

## 完成后检查

每次完成修改后，至少检查：

- 本地页面能正常打开。
- 图片、PDF、站内锚点和外部链接正常。
- 首页主要区块在手机和桌面下不重叠、不溢出。
- 构建或预览没有明显报错。
- 若涉及部署，确认路径仍适合 GitHub Pages。

