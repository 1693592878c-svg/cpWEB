# 测试与排查说明

这份文档解释什么是测试，以及个人网站项目应该怎么让 Codex 自己排查问题。

## 什么是单元测试

单元测试是对“一个很小的功能”做自动检查。

例如：

- 输入一个项目分类，应该筛选出对应项目。
- 输入一个邮箱地址，应该生成正确的邮件链接。
- 一个函数接收项目数据，应该返回正确的标签列表。

它的价值是：以后改代码时，自动提醒你有没有把原本正常的功能改坏。

## 个人网站需要哪些测试

个人网站不一定一开始就写很多单元测试。更实用的是分层检查。

## 第一层：人工验收

适合所有修改。

每次改完至少检查：

- 首页是否能打开。
- 导航锚点是否能跳转。
- 项目卡片是否显示完整。
- 图片是否显示。
- PDF 是否能打开。
- 手机端是否正常。
- 联系电话和邮箱是否可点击。

## 第二层：构建检查

适合 React/Vite 项目。

目标：

- 确认项目能成功打包。
- 提前发现语法错误、资源引用错误、构建配置问题。

验收标准：

- 构建过程没有报错。
- 构建后的页面可以预览。
- 构建后图片、样式、PDF 仍然正常。

## 第三层：页面测试

适合检查真实用户操作。

可以让 Codex 或浏览器测试工具模拟：

- 打开首页。
- 点击“案例作品”。
- 点击“联系我”。
- 打开简历 PDF。
- 打开作品集 PDF。
- 切换手机尺寸查看页面。

这种测试比单元测试更适合个人作品集网站，因为它直接检查用户看到的结果。

## 第四层：单元测试

适合当项目里出现较多逻辑时再补充，例如：

- 项目筛选。
- 搜索。
- 表单校验。
- 数据格式转换。
- 多语言切换。

如果只是静态展示型页面，单元测试优先级可以低一些。

## Bug 排查路径

遇到 Bug 时，按这个顺序排查：

1. 复现问题：确认在哪个页面、什么操作后出现。
2. 看预期：明确原本应该发生什么。
3. 看范围：是本地有问题，还是部署后才有问题。
4. 查控制台：是否有脚本或资源加载错误。
5. 查路径：图片、PDF、CSS、JS 是否引用正确。
6. 查样式：是否是布局、间距、层级、宽度导致显示异常。
7. 查数据：项目标题、图片路径、标签是否写错。
8. 修复后复测：不要只改代码，要重新看页面结果。

## 给 Codex 的 Bug 描述模板

```text
我在个人网站遇到一个问题：

现象：
具体看到什么，例如图片不显示、按钮没反应、手机端错位。

出现位置：
哪个页面、哪个区块、什么操作后出现。

预期：
我希望它应该是什么样。

环境：
本地预览正常/不正常；GitHub Pages 正常/不正常。

请你：
自己排查可能原因，检查代码、路径、样式和构建配置；给出修复并复测，不要只让我逐条指出。
```

## Codex 可以主动检查什么

你可以要求 Codex 主动完成：

- 检查文件结构。
- 检查资源路径。
- 检查构建配置。
- 检查页面样式冲突。
- 检查移动端布局。
- 检查浏览器控制台报错。
- 检查 GitHub Pages 部署风险。
- 给出修复记录和后续注意事项。

## 什么时候需要真正写测试代码

建议在这些情况写测试代码：

- 加了项目筛选、搜索、排序。
- 加了表单。
- 加了复杂交互。
- 加了多页面路由。
- 项目准备长期维护。
- 每次修改都容易引发旧问题。

## 每次交给 Codex 的验收要求

可以固定加这句：

```text
完成后请你自己检查：页面能否打开、图片和 PDF 是否正常、手机端是否错位、构建是否通过、是否影响 GitHub Pages 部署。
```

